# --ignore-errors
# --no-playlist

# Save in ~/Videos
-o ~/Videos/%(title)s.%(ext)s

# Prefer 480p or lower resolutions
-f bestvideo[ext=mp4][width<2000][height<=480]+bestaudio[ext=m4a]/bestvideo[ext=webm][width<2000][height<=480]+bestaudio[ext=webm]/bestvideo[width<2000][height<=480]+bestaudio/best[width<2000][height<=480]/best
